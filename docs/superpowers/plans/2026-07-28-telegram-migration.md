# Telegram (Python/PTB) Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Переключить генератор конструктора так, чтобы он компилировал бота в Python на базе python-telegram-bot (PTB) под Telegram (полная замена Max/Node).

**Architecture:** Граф-модель (`store.js`, `nodeFactory.js`) и BFS-обход (`traverse.js`) сохраняются. Общий Python-пролог бота (таблицы, `render`, `handle`, разметка клавиатур, `build_app`) генерирует новый `src/generator/runtime.js` (JS-модуль, возвращающий Python-строку). Транспорты `polling.js`/`webhook.js` добавляют `run_polling`/`run_webhook`. Поведение бота проверяется pytest-тестами, которые генерируют `bot.py` через `node scripts/generate.mjs` и исполняют `handle()` с фейковыми апдейтами. Структура вывода проверяется на vitest.

**Tech Stack:** Генератор — JS/ESM. Выход — Python 3.10+ / python-telegram-bot v21+. Тесты — vitest (JS) + pytest (Python).

## Global Constraints

- Выходной `bot.py`: Python 3.10+, python-telegram-bot v21+. В шапке — `# pip install python-telegram-bot`.
- Запуск бота — строго под `if __name__ == "__main__":` (импорт для тестов не поднимает Application).
- Токен: `TOKEN = os.environ.get("BOT_TOKEN") or <вшитый>`.
- Таблицы встраиваются как `_TABLES = json.loads(<json-строка>)` (двойное кодирование в генераторе), чтобы избежать `true/false/null` и проблем экранирования.
- Бренд-баннер: `# Generated by Bot Constructor for Telegram`.
- Состояние — обычные `dict` в памяти, без PTB persistence/ConversationHandler.
- Payload кнопок сообщений: `btn_0`, `btn_1`, … (из `traverse`, не меняется).
- Исходники `src/` и `scripts/` — ESM. `package.json` получает `"type": "module"`.

---

### Task 1: traverse.js — refusal-кнопка без payload

Telegram-кнопка отказа (reply-клавиатура) шлёт свой текст, а не callback. `traverse` должен отдавать `refusalButton: { text }` без `payload`.

**Files:**
- Modify: `src/generator/traverse.js:60-62`
- Test: `tests/generator/traverse.test.js:108-136`

**Interfaces:**
- Produces: `authPrompts[id].refusalButton` теперь `{ text: string }` или `null`. Транзиция `transitions[id].refused` без изменений.

- [ ] **Step 1: Обновить тест на refusal (сделать красным)**

В `tests/generator/traverse.test.js`, тест `'emits refusal button and refused transition when enabled'`, заменить ожидание:

```js
    expect(authPrompts.a1).toEqual({
      promptText: 'P',
      contactButton: { text: 'C' },
      refusalButton: { text: 'R' },
    });
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run tests/generator/traverse.test.js`
Expected: FAIL — фактический `refusalButton` содержит `payload: 'auth_refuse_a1'`.

- [ ] **Step 3: Убрать payload**

В `src/generator/traverse.js` заменить:

```js
      const refusalButton = node.data.refusalEnabled
        ? { text: refusalText, payload: `auth_refuse_${id}` }
        : null;
```

на:

```js
      const refusalButton = node.data.refusalEnabled
        ? { text: refusalText }
        : null;
```

- [ ] **Step 4: Запустить — убедиться, что зелёные**

Run: `npx vitest run tests/generator/traverse.test.js`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/generator/traverse.js tests/generator/traverse.test.js
git commit -m "refactor(traverse): drop callback payload from auth refusal button"
```

---

### Task 2: runtime.js + polling.js — Python-пролог и long-polling (PTB)

Создаём `runtime.js` (общий Python-пролог, включая `build_app`) и переписываем `polling.js` под `run_polling`. Структурные проверки вывода — на vitest (исполнение Python-логики проверяется в Task 5).

**Files:**
- Create: `src/generator/runtime.js`
- Rewrite: `src/generator/polling.js`
- Rewrite (test): `tests/generator/polling.test.js`

**Interfaces:**
- Consumes: `{ token, messages, authPrompts, transitions, initialNext }` (результат `traverse` + `token`).
- Produces:
  - `botRuntime(args): string` — Python-пролог: `TOKEN`, таблицы, `render`, `message_markup`, `auth_markup`, `send`, `handle`, `build_app`.
  - `generatePolling(args): string` — `botRuntime(args)` + `main()`/`run_polling` под `__main__`.

- [ ] **Step 1: Переписать `tests/generator/polling.test.js` целиком (сделать красным)**

```js
import { describe, it, expect } from 'vitest';
import { generatePolling } from '../../src/generator/polling.js';
import { generate } from '../../src/generator/index.js';

const sample = {
  token: 'tok123',
  messages: { m1: { text: 'hi', buttons: [{ text: 'Y', payload: 'btn_0' }] } },
  authPrompts: {},
  transitions: { m1: { btn_0: null } },
  initialNext: 'm1',
};

describe('generatePolling — Python/PTB output', () => {
  it('emits a PTB long-polling bot', () => {
    const code = generatePolling(sample);
    expect(code).toContain('# Generated by Bot Constructor for Telegram');
    expect(code).toContain('# pip install python-telegram-bot');
    expect(code).toContain('from telegram.ext import');
    expect(code).toContain('def build_app():');
    expect(code).toContain('app.run_polling()');
    expect(code).toContain('if __name__ == "__main__":');
    expect(code).toContain('os.environ.get("BOT_TOKEN")');
    expect(code).toContain('"tok123"');
  });

  it('embeds tables via json.loads including node ids and payloads', () => {
    const code = generatePolling(sample);
    expect(code).toContain('_TABLES = json.loads(');
    expect(code).toContain('m1');
    expect(code).toContain('btn_0');
  });
});

describe('generate(project)', () => {
  const project = (mode) => ({
    token: 'T', mode,
    nodes: [
      { id: 'start', type: 'start', data: {} },
      { id: 'm1', type: 'message', data: { text: 'hi', buttonsEnabled: false } },
    ],
    edges: [{ id: 'e0', source: 'start', target: 'm1' }],
  });
  it('dispatches to polling template for mode: "polling"', () => {
    expect(generate(project('polling'))).toContain('run_polling');
  });
  it('dispatches to webhook template for mode: "webhook"', () => {
    expect(generate(project('webhook'))).toContain('run_webhook');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run tests/generator/polling.test.js`
Expected: FAIL — старый `polling.js` эмитит JS для Max.

- [ ] **Step 3: Создать `src/generator/runtime.js`**

```js
export function botRuntime({ token, messages, authPrompts = {}, transitions, initialNext }) {
  const tables = {
    MESSAGES: messages,
    AUTH_PROMPTS: authPrompts,
    TRANSITIONS: transitions,
    INITIAL_NEXT: initialNext,
  };
  const tablesLiteral = JSON.stringify(JSON.stringify(tables));

  return `# Generated by Bot Constructor for Telegram
# pip install python-telegram-bot
# Состояние (user_state) и переменные (user_vars) хранятся в памяти процесса и теряются при перезапуске.
import os
import re
import json

from telegram import (
    InlineKeyboardButton,
    InlineKeyboardMarkup,
    KeyboardButton,
    ReplyKeyboardMarkup,
    ReplyKeyboardRemove,
)
from telegram.ext import (
    Application,
    CommandHandler,
    MessageHandler,
    CallbackQueryHandler,
    filters,
)

TOKEN = os.environ.get("BOT_TOKEN") or ${JSON.stringify(token)}

_TABLES = json.loads(${tablesLiteral})
MESSAGES = _TABLES["MESSAGES"]
AUTH_PROMPTS = _TABLES["AUTH_PROMPTS"]
TRANSITIONS = _TABLES["TRANSITIONS"]
INITIAL_NEXT = _TABLES["INITIAL_NEXT"]

user_state = {}
user_vars = {}


def render(text, chat_id):
    vars_ = user_vars.get(chat_id, {})
    return re.sub(r"\\{\\{(\\w+)\\}\\}", lambda m: str(vars_.get(m.group(1), "")), str(text))


def message_markup(msg):
    buttons = msg.get("buttons")
    if not buttons:
        return ReplyKeyboardRemove()
    rows = [[InlineKeyboardButton(b["text"], callback_data=b["payload"])] for b in buttons]
    return InlineKeyboardMarkup(rows)


def auth_markup(prompt):
    rows = [[KeyboardButton(prompt["contactButton"]["text"], request_contact=True)]]
    if prompt.get("refusalButton"):
        rows.append([KeyboardButton(prompt["refusalButton"]["text"])])
    return ReplyKeyboardMarkup(rows, resize_keyboard=True, one_time_keyboard=True)


async def send(context, chat_id, text, reply_markup):
    await context.bot.send_message(chat_id=chat_id, text=text, reply_markup=reply_markup)


async def handle(update, context):
    if update.callback_query:
        await update.callback_query.answer()
    chat_id = update.effective_chat.id
    message = update.message
    text = message.text if message else None
    contact = message.contact if message else None
    data = update.callback_query.data if update.callback_query else None

    if text == "/start":
        user_state[chat_id] = "start"
    state = user_state.get(chat_id, "start")
    nxt = None

    if state == "start":
        nxt = INITIAL_NEXT
    elif state in AUTH_PROMPTS:
        prompt = AUTH_PROMPTS[state]
        if contact:
            prev = user_vars.get(chat_id, {})
            user_vars[chat_id] = {
                **prev,
                "first_name": contact.first_name or "",
                "last_name": contact.last_name or "",
                "phone": str(contact.phone_number or ""),
            }
            nxt = TRANSITIONS.get(state, {}).get("contact")
        elif prompt.get("refusalButton") and text == prompt["refusalButton"]["text"]:
            nxt = TRANSITIONS.get(state, {}).get("refused")
        else:
            return
    elif state in MESSAGES:
        if contact:
            return
        trans = TRANSITIONS.get(state)
        if not trans:
            return
        nxt = trans.get(data)
        if nxt is None:
            nxt = trans.get("default")
    else:
        return

    if not nxt:
        user_state[chat_id] = "start"
        return

    if nxt in AUTH_PROMPTS:
        prompt = AUTH_PROMPTS[nxt]
        await send(context, chat_id, render(prompt["promptText"], chat_id), auth_markup(prompt))
    elif nxt in MESSAGES:
        m = MESSAGES[nxt]
        await send(context, chat_id, render(m["text"], chat_id), message_markup(m))
    else:
        user_state[chat_id] = "start"
        return

    next_trans = TRANSITIONS.get(nxt)
    has_any = bool(next_trans) and any(next_trans.values())
    user_state[chat_id] = nxt if has_any else "start"


def build_app():
    app = Application.builder().token(TOKEN).build()
    app.add_handler(CommandHandler("start", handle))
    app.add_handler(MessageHandler(filters.CONTACT, handle))
    app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle))
    app.add_handler(CallbackQueryHandler(handle))
    return app
`;
}
```

Примечание по экранированию: в JS-шаблоне `\\{` даёт в выводе `\{`, `\\w` → `\w`. Итоговая Python-строка регэкспа — `r"\{\{(\w+)\}\}"`.

- [ ] **Step 4: Переписать `src/generator/polling.js` целиком**

```js
import { botRuntime } from './runtime.js';

export function generatePolling(args) {
  return `${botRuntime(args)}

def main():
    app = build_app()
    print("Бот запущен (Telegram, long polling). Ожидание сообщений...")
    app.run_polling()


if __name__ == "__main__":
    main()
`;
}
```

- [ ] **Step 5: Запустить — убедиться, что зелёные**

Run: `npx vitest run tests/generator/polling.test.js`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/generator/runtime.js src/generator/polling.js tests/generator/polling.test.js
git commit -m "feat(generator): Python/PTB runtime + long-polling transport"
```

---

### Task 3: webhook.js — Python webhook-транспорт (PTB)

Переписываем `webhook.js` поверх общего `botRuntime`: PTB сам поднимает HTTP-сервер через `run_webhook`.

**Files:**
- Rewrite: `src/generator/webhook.js`
- Rewrite (test): `tests/generator/webhook.test.js`

**Interfaces:**
- Consumes: `botRuntime(args)` из Task 2.
- Produces: `generateWebhook(args): string` — `botRuntime(args)` + `main()`/`run_webhook` под `__main__`.

- [ ] **Step 1: Переписать `tests/generator/webhook.test.js` целиком (сделать красным)**

```js
import { describe, it, expect } from 'vitest';
import { generateWebhook } from '../../src/generator/webhook.js';

const sample = {
  token: 'T',
  messages: {},
  authPrompts: { a1: { promptText: 'p', contactButton: { text: 'c' }, refusalButton: null } },
  transitions: { a1: { contact: null } },
  initialNext: 'a1',
};

describe('generateWebhook — Python/PTB output', () => {
  it('emits a PTB webhook bot on port 3000', () => {
    const code = generateWebhook(sample);
    expect(code).toContain('# Generated by Bot Constructor for Telegram');
    expect(code).toContain('def build_app():');
    expect(code).toContain('app.run_webhook(');
    expect(code).toContain('port=3000');
    expect(code).toContain('if __name__ == "__main__":');
  });

  it('carries the shared runtime (tables, auth, request_contact)', () => {
    const code = generateWebhook(sample);
    expect(code).toContain('_TABLES = json.loads(');
    expect(code).toContain('AUTH_PROMPTS');
    expect(code).toContain('request_contact=True');
    expect(code).toContain('def render(');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run tests/generator/webhook.test.js`
Expected: FAIL — старый `webhook.js` эмитит JS `http.createServer` для Max.

- [ ] **Step 3: Переписать `src/generator/webhook.js` целиком**

```js
import { botRuntime } from './runtime.js';

export function generateWebhook(args) {
  return `${botRuntime(args)}

def main():
    app = build_app()
    # Установите публичный HTTPS-URL вебхука в переменной окружения WEBHOOK_URL.
    webhook_url = os.environ.get("WEBHOOK_URL", "")
    print("Бот запущен (Telegram, webhook) на порту 3000...")
    app.run_webhook(
        listen="0.0.0.0",
        port=3000,
        url_path=TOKEN,
        webhook_url=webhook_url,
    )


if __name__ == "__main__":
    main()
`;
}
```

- [ ] **Step 4: Запустить — убедиться, что зелёные**

Run: `npx vitest run tests/generator/webhook.test.js`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/generator/webhook.js tests/generator/webhook.test.js
git commit -m "feat(generator): Python/PTB webhook transport on shared runtime"
```

---

### Task 4: Метаданные проекта (ESM, имя, документация)

Включить `"type": "module"` (нужно для запуска генератора через `node` из pytest), переименовать пакет, обновить Max-специфичные строки в `CLAUDE.md`.

**Files:**
- Modify: `package.json:2`, `package.json` (добавить `type`)
- Modify: `CLAUDE.md` (заголовок, описание, раздел генерации, стек)

**Interfaces:** только метаданные/докстроки.

- [ ] **Step 1: Обновить `package.json`**

Заменить:

```json
  "name": "bot-constructor-max",
  "version": "0.1.1",
  "private": true,
```

на:

```json
  "name": "bot-constructor-telegram",
  "version": "0.1.1",
  "private": true,
  "type": "module",
```

- [ ] **Step 2: Прогнать vitest — убедиться, что `type: module` ничего не сломал**

Run: `npx vitest run`
Expected: PASS (store, traverse, polling, webhook — структурные).

- [ ] **Step 3: Обновить идентичность и стек в `CLAUDE.md`**

Заменить заголовок:

```
# Bot Constructor для Max — Project Context
```

на:

```
# Bot Constructor для Telegram — Project Context
```

Заменить описание:

```
Десктопное EXE-приложение — визуальный блочный конструктор ботов для мессенджера Max (https://dev.max.ru/docs-api). Пользователь строит сценарий бота на граф-холсте и компилирует в готовый Node.js-скрипт.
```

на:

```
Десктопное EXE-приложение — визуальный блочный конструктор ботов для мессенджера Telegram (https://core.telegram.org/bots/api). Пользователь строит сценарий бота на граф-холсте и компилирует в готовый Python-скрипт на python-telegram-bot.
```

Заменить строку стека:

```
| Генерируемый код | Node.js 18+ (нативный fetch, без зависимостей) |
```

на:

```
| Генерируемый код | Python 3.10+ (python-telegram-bot v21+) |
```

- [ ] **Step 4: Обновить раздел «Генерация bot.js» в `CLAUDE.md`**

Заменить блок:

```
**Long Polling:** бесконечный цикл `GET /updates?token=...&timeout=30&marker=...`

**Webhook:** `http.createServer` на порту 3000, принимает POST от Max API

Скрипт запускается как `node bot.js` без npm install (Node.js 18+).
```

на:

```
**Long Polling:** `Application.run_polling()` из python-telegram-bot

**Webhook:** `Application.run_webhook(port=3000, ...)`; публичный HTTPS-URL берётся из `WEBHOOK_URL`

Скрипт запускается как `python bot.py` (нужен `pip install python-telegram-bot`). Токен — из `BOT_TOKEN` или вшитый.
```

- [ ] **Step 5: Commit**

```bash
git add package.json CLAUDE.md
git commit -m "chore: ESM package + rebrand project docs to Telegram/Python"
```

---

### Task 5: Поведенческие pytest-тесты сгенерированного бота

CLI-обёртка генератора + pytest, который генерирует `bot.py` через `node` и исполняет `handle()` с фейковыми апдейтами.

**Files:**
- Create: `scripts/generate.mjs`
- Create: `tests/py/conftest.py`
- Create: `tests/py/test_bot.py`

**Interfaces:**
- `scripts/generate.mjs`: `node scripts/generate.mjs <project.json>` → печатает Python-код `generate(project)` в stdout.
- `conftest.make_bot(project: dict)` → импортированный модуль сгенерированного бота (`handle`, `user_state`, `user_vars`).

**Setup (выполнить один раз перед шагами):**

Run: `python -m pip install "python-telegram-bot>=21" pytest`
Expected: зависимости установлены (Python 3.10+).

- [ ] **Step 1: Создать `scripts/generate.mjs`**

```js
import { readFileSync } from 'node:fs';
import { generate } from '../src/generator/index.js';

const path = process.argv[2];
if (!path) {
  console.error('usage: node scripts/generate.mjs <project.json>');
  process.exit(1);
}
const project = JSON.parse(readFileSync(path, 'utf8'));
process.stdout.write(generate(project));
```

- [ ] **Step 2: Проверить CLI вручную (санити)**

Run: `node -e "import('./src/generator/index.js').then(m=>process.stdout.write(m.generate({token:'T',mode:'polling',nodes:[{id:'start',type:'start',data:{}},{id:'m1',type:'message',data:{text:'hi',buttonsEnabled:false}}],edges:[{id:'e0',source:'start',target:'m1'}]})))" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>console.log(s.includes('run_polling')?'OK':'MISSING'))"`
Expected: `OK`.

- [ ] **Step 3: Создать `tests/py/conftest.py`**

```python
import importlib.util
import json
import os
import subprocess
import tempfile
import uuid

import pytest

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))


def _generate(project):
    fd, project_path = tempfile.mkstemp(suffix=".json")
    with os.fdopen(fd, "w", encoding="utf-8") as f:
        json.dump(project, f)
    try:
        result = subprocess.run(
            ["node", os.path.join("scripts", "generate.mjs"), project_path],
            cwd=ROOT,
            capture_output=True,
            text=True,
            encoding="utf-8",
        )
    finally:
        os.unlink(project_path)
    if result.returncode != 0:
        raise RuntimeError(f"generate.mjs failed:\n{result.stderr}")
    return result.stdout


def _import_source(source):
    path = os.path.join(tempfile.gettempdir(), f"genbot_{uuid.uuid4().hex}.py")
    with open(path, "w", encoding="utf-8") as f:
        f.write(source)
    name = os.path.splitext(os.path.basename(path))[0]
    spec = importlib.util.spec_from_file_location(name, path)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


@pytest.fixture
def make_bot():
    def _make(project):
        return _import_source(_generate(project))
    return _make
```

- [ ] **Step 4: Создать `tests/py/test_bot.py` с одним тестом (сделать красным)**

```python
import asyncio
from types import SimpleNamespace
from unittest.mock import AsyncMock

from telegram import InlineKeyboardMarkup, ReplyKeyboardMarkup, ReplyKeyboardRemove


def run(coro):
    return asyncio.run(coro)


def make_context():
    return SimpleNamespace(bot=SimpleNamespace(send_message=AsyncMock()))


def make_update(chat_id, text=None, contact=None, callback_data=None):
    message = None
    if text is not None or contact is not None:
        message = SimpleNamespace(text=text, contact=contact)
    callback_query = None
    if callback_data is not None:
        callback_query = SimpleNamespace(data=callback_data, answer=AsyncMock())
    return SimpleNamespace(
        message=message,
        callback_query=callback_query,
        effective_chat=SimpleNamespace(id=chat_id),
    )


def sent(ctx):
    return [c.kwargs for c in ctx.bot.send_message.call_args_list]


def linear_buttons_project():
    return {
        "token": "T", "mode": "polling",
        "nodes": [
            {"id": "start", "type": "start", "data": {}},
            {"id": "m1", "type": "message", "data": {
                "text": "hi", "buttonsEnabled": True,
                "buttons": [{"text": "Y"}, {"text": "N"}]}},
            {"id": "m2", "type": "message", "data": {"text": "bye", "buttonsEnabled": False}},
        ],
        "edges": [
            {"id": "e0", "source": "start", "target": "m1"},
            {"id": "e1", "source": "m1", "sourceHandle": "btn-0", "target": "m2"},
        ],
    }


def test_start_routes_to_initial_message_with_inline_keyboard(make_bot):
    bot = make_bot(linear_buttons_project())
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    calls = sent(ctx)
    assert calls[0]["chat_id"] == 42
    assert calls[0]["text"] == "hi"
    markup = calls[0]["reply_markup"]
    assert isinstance(markup, InlineKeyboardMarkup)
    btn = markup.inline_keyboard[0][0]
    assert btn.text == "Y"
    assert btn.callback_data == "btn_0"
    assert bot.user_state[42] == "m1"
```

- [ ] **Step 5: Запустить — убедиться, что зелёный**

Run: `python -m pytest tests/py/test_bot.py -v`
Expected: PASS (генератор и рантайм уже реализованы в Tasks 2–3; тест подтверждает поведение).

- [ ] **Step 6: Дописать остальные поведенческие тесты в `tests/py/test_bot.py`**

Добавить в конец файла:

```python
def test_callback_advances_and_removes_keyboard(make_bot):
    bot = make_bot(linear_buttons_project())
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    run(bot.handle(make_update(42, callback_data="btn_0"), ctx))
    calls = sent(ctx)
    assert calls[1]["text"] == "bye"
    assert isinstance(calls[1]["reply_markup"], ReplyKeyboardRemove)
    assert bot.user_state[42] == "start"  # m2 терминальна


def test_start_command_resets_mid_flow(make_bot):
    project = {
        "token": "T", "mode": "polling",
        "nodes": [
            {"id": "start", "type": "start", "data": {}},
            {"id": "m1", "type": "message", "data": {"text": "first", "buttonsEnabled": False}},
            {"id": "m2", "type": "message", "data": {"text": "second", "buttonsEnabled": False}},
        ],
        "edges": [
            {"id": "e0", "source": "start", "target": "m1"},
            {"id": "e1", "source": "m1", "target": "m2"},
        ],
    }
    bot = make_bot(project)
    ctx = make_context()
    run(bot.handle(make_update(7, text="/start"), ctx))
    assert bot.user_state[7] == "m1"
    run(bot.handle(make_update(7, text="/start"), ctx))
    assert sent(ctx)[1]["text"] == "first"
    assert bot.user_state[7] == "m1"


def test_variable_substitution(make_bot):
    project = {
        "token": "T", "mode": "polling",
        "nodes": [
            {"id": "start", "type": "start", "data": {}},
            {"id": "m1", "type": "message",
             "data": {"text": "Hi {{first_name}} {{last_name}}!", "buttonsEnabled": False}},
        ],
        "edges": [{"id": "e0", "source": "start", "target": "m1"}],
    }
    bot = make_bot(project)
    ctx = make_context()
    bot.user_vars[42] = {"first_name": "Иван", "last_name": "Петров"}
    run(bot.handle(make_update(42, text="/start"), ctx))
    assert sent(ctx)[0]["text"] == "Hi Иван Петров!"


def test_unknown_variable_renders_empty(make_bot):
    project = {
        "token": "T", "mode": "polling",
        "nodes": [
            {"id": "start", "type": "start", "data": {}},
            {"id": "m1", "type": "message",
             "data": {"text": "X{{missing}}Y", "buttonsEnabled": False}},
        ],
        "edges": [{"id": "e0", "source": "start", "target": "m1"}],
    }
    bot = make_bot(project)
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    assert sent(ctx)[0]["text"] == "XY"


def auth_project(refusal=False):
    data = {
        "promptText": "Поделитесь", "contactButtonText": "Контакт",
        "refusalEnabled": refusal, "refusalButtonText": "Отказаться",
    }
    edges = [
        {"id": "e0", "source": "start", "target": "a1"},
        {"id": "e1", "source": "a1", "sourceHandle": "contact", "target": "m1"},
    ]
    nodes = [
        {"id": "start", "type": "start", "data": {}},
        {"id": "a1", "type": "auth", "data": data},
        {"id": "m1", "type": "message",
         "data": {"text": "Привет, {{first_name}}!", "buttonsEnabled": False}},
        {"id": "m2", "type": "message", "data": {"text": "reject", "buttonsEnabled": False}},
    ]
    if refusal:
        edges.append({"id": "e2", "source": "a1", "sourceHandle": "refused", "target": "m2"})
    return {"token": "T", "mode": "polling", "nodes": nodes, "edges": edges}


def test_auth_prompt_uses_request_contact_reply_keyboard(make_bot):
    bot = make_bot(auth_project(refusal=True))
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    call = sent(ctx)[0]
    assert call["text"] == "Поделитесь"
    markup = call["reply_markup"]
    assert isinstance(markup, ReplyKeyboardMarkup)
    assert markup.keyboard[0][0].text == "Контакт"
    assert markup.keyboard[0][0].request_contact is True
    assert markup.keyboard[1][0].text == "Отказаться"
    assert bot.user_state[42] == "a1"


def test_contact_stores_vars_and_advances(make_bot):
    bot = make_bot(auth_project())
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    contact = SimpleNamespace(
        first_name="Иван", last_name="Петров", phone_number="+71234567890")
    run(bot.handle(make_update(42, contact=contact), ctx))
    assert bot.user_vars[42] == {
        "first_name": "Иван", "last_name": "Петров", "phone": "+71234567890"}
    assert sent(ctx)[1]["text"] == "Привет, Иван!"


def test_refusal_by_text_advances(make_bot):
    bot = make_bot(auth_project(refusal=True))
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    run(bot.handle(make_update(42, text="Отказаться"), ctx))
    assert sent(ctx)[1]["text"] == "reject"
    assert bot.user_state[42] == "start"


def test_arbitrary_text_ignored_while_waiting_for_contact(make_bot):
    bot = make_bot(auth_project())
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    before = len(sent(ctx))
    run(bot.handle(make_update(42, text="привет"), ctx))
    assert len(sent(ctx)) == before
    assert bot.user_state[42] == "a1"


def test_contact_ignored_in_message_state(make_bot):
    project = {
        "token": "T", "mode": "polling",
        "nodes": [
            {"id": "start", "type": "start", "data": {}},
            {"id": "m1", "type": "message", "data": {"text": "first", "buttonsEnabled": False}},
            {"id": "m2", "type": "message", "data": {"text": "second", "buttonsEnabled": False}},
        ],
        "edges": [
            {"id": "e0", "source": "start", "target": "m1"},
            {"id": "e1", "source": "m1", "target": "m2"},
        ],
    }
    bot = make_bot(project)
    ctx = make_context()
    run(bot.handle(make_update(42, text="/start"), ctx))
    assert bot.user_state[42] == "m1"
    before = len(sent(ctx))
    contact = SimpleNamespace(first_name="X", last_name="Y", phone_number="+1")
    run(bot.handle(make_update(42, contact=contact), ctx))
    assert len(sent(ctx)) == before
    assert bot.user_state[42] == "m1"
```

- [ ] **Step 7: Запустить весь pytest-набор — убедиться, что зелёный**

Run: `python -m pytest tests/py -v`
Expected: PASS (все поведенческие тесты).

- [ ] **Step 8: Прогнать оба набора тестов**

Run: `npx vitest run` затем `python -m pytest tests/py -v`
Expected: оба PASS.

- [ ] **Step 9: Commit**

```bash
git add scripts/generate.mjs tests/py/conftest.py tests/py/test_bot.py
git commit -m "test(py): behavioral pytest for generated Telegram/PTB bot"
```

---

## Self-Review

**Spec coverage:**
- Полная замена Max→Telegram/Python, PTB → Tasks 2–4.
- Маппинг (send_message, InlineKeyboardMarkup, ReplyKeyboardMarkup+request_contact, callback_query, run_polling/run_webhook, ReplyKeyboardRemove) → runtime.js (Task 2), webhook.js (Task 3).
- Auth через reply-клавиатуру, contact из `message.contact`, отказ по тексту, удаление VCF → Task 1 (traverse) + Task 2 (`auth_markup`, `handle`).
- Токен из env с fallback → runtime.js.
- `/start` глобальный сброс → `handle` (Task 2), тест `test_start_command_resets_mid_flow`.
- Скрытие клавиатуры → `message_markup` → `ReplyKeyboardRemove`, тест `test_callback_advances_and_removes_keyboard`.
- Встраивание таблиц через `json.loads` → runtime.js, проверка в vitest (Task 2) и через исполнение (Task 5).
- Точка входа под `__main__` → polling.js/webhook.js, косвенно подтверждается тем, что импорт в pytest не поднимает бота.
- Поведенческие pytest + CLI-генерация → Task 5. Структурные vitest → Tasks 2–3. `store.test.js` не трогается.
- ESM (`type: module`) для запуска генератора из pytest → Task 4.

**Placeholder scan:** нет TBD/TODO; весь код и тесты приведены дословно.

**Type consistency:** `botRuntime` определяет `render`, `message_markup`, `auth_markup`, `send`, `handle`, `build_app` — `polling.js`/`webhook.js` вызывают `build_app()`; pytest использует `handle`, `user_state`, `user_vars`. `traverse` отдаёт `refusalButton: { text }` (Task 1) → `auth_markup`/`handle` читают `prompt["refusalButton"]["text"]` (Task 2). `messages[id].buttons` = `{ text, payload }` → `message_markup` читает `b["text"]`/`b["payload"]`. `generate()` (index.js, без изменений) диспетчеризует по `project.mode` в `generatePolling`/`generateWebhook`.
```
